mutation ($input: UpdateTenantTierInput!) {
  updateTenantTier(input: $input) {
    tenantTierMembership {
      id
      tierId
    }
    error {
      message
      type
      code
      fields {
        field
        message
        type
      }
    }
  }
}
